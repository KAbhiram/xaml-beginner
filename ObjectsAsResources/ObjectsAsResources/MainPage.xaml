<Page
    x:Class="ObjectsAsResources.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ObjectsAsResources"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:models="using:DataApp.Models">
    <Page.Resources>
        <models:TitleElement x:Key="Title"/>
        <models:SubTitleElement x:Key="SubTitle"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button HorizontalAlignment="Left"
                Content="{StaticResource Title}"/>
        <Button HorizontalAlignment="Right"
                Content="{StaticResource SubTitle}"/>
        <Button HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Content="{StaticResource Title}"/>
        <Button HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Content="{StaticResource SubTitle}"/>
    </Grid>
</Page>
