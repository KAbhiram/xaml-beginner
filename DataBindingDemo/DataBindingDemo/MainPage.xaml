<Page
    x:Class="DataBindingDemo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DataBindingDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:DataBindingDemo.Models"
    mc:Ignorable="d">
    <Page.Resources>
        <x:String x:Key="NullPlaceholder">Value is null</x:String>
        <x:String x:Key="FallbackPlaceholder">Wrong Property Name</x:String>
    </Page.Resources>
    <Page.DataContext>
        <models:Person FirstName="Jai"/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock>
            <TextBlock.Text>
                <Binding Path="LastName" >
                    <Binding.Source>
                        <models:Person FirstName="Kango" LastName="Abhiram"/>
                    </Binding.Source>
                </Binding>
            </TextBlock.Text>
        </TextBlock>
    </Grid>
</Page>
